const anji=["coreJava","AdvancedJava","Oracle","JavaFrameworks","WebTechnologies","WebFrameworks"];
const veeru="course";
const newArray=[];
for(let i=0; i<=anji.length-1; i++)
{
    newArray.push(`${anji[i]} ${veeru}`);
}
console.log(newArray);